export let text = (size = "16px", color = "inherit", weight = "normal", align = "left", font = "sans-serif") => ({
    fontSize: size,
    color: color,
    fontWeight: weight,
    textAlign: align,
    fontFamily: font,
});
export let textStyle = ({size = "16px", color = "inherit", weight = "normal", align = "left", font = "sans-serif"}) => ({
    fontSize: size,
    color: color,
    fontWeight: weight,
    textAlign: align,
    fontFamily: font,
});
