export let kf_floatTop = {
        "0%": {
            opacity: 0,
            transform: `translateY(100%)`
        },

        "100%": {
            opacity: 1,
            transform: "translateY(0)"}
}

export let floatTop = {
        animation: `floatTop 1s ease-in-out forwards`,
}
